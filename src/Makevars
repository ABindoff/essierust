# Linux / macOS build.
LIBDIR  = ./rust/target/release
STATLIB = $(LIBDIR)/libessierust.a

PKG_LIBS = -L$(LIBDIR) -lessierust -lm

all: C_clean

$(SHLIB): $(STATLIB)

$(STATLIB):
	cargo build --lib --release --manifest-path=./rust/Cargo.toml

C_clean:
	rm -Rf $(SHLIB) $(STATLIB) $(OBJECTS)
